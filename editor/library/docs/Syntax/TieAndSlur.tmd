# 连音线

+ 连音线分为延音线和圆滑线，均用音符（或和弦）间的`^`表示。
+ 延音线用在音高相同的两个音符之间，表示将这两个音符时值相加作为一个音符演奏。
+ 圆滑线用在音高不同的两个音符之间，表示单独但连贯地演奏。
+ 对于多个音符之间的连音线，需要在每两个音符之间都加上连音线`^`。
+ 连音线会可以作用在两个被小节线分隔的音符之间，此时应将连音线写在第一个音符之后，小节线之前。

^ 范例
  ^ 基本范例 ^

两个音符和多个音符之间的连音线（此处为圆滑线）：
```
(2/4)3_3=5=6=^5=1'=^6=|5=^3=^5^6_|
```

连音线（此处为跨小节的延音线）和连音符是两种不同的符号：
```
3_|5---^|5_6_6_.5=5_.3=(3~){3_2_1_}|3---|
```

  ^ 使用实例

  ^ 推广与延伸

^ 参见 ^

+ [音符|Note] + [和弦|Chord] + [小节线|BarLine] + [时值操作符|DurationOperator] +