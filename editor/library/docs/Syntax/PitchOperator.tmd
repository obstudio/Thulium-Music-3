# 音高操作符

+ 音高操作符写在一个音符（或和弦）的后方，用于改变其音高。音高操作符有`'`、`,`、`#`、`b`四个。
+ `'`将一个音升高一个八度，`,`将一个音降低一个八度。
+ `#`将一个音升高一个半音，`b`将一个音降低一个半音。

^ 范例
  ^ 基本范例 ^

一个音阶：
```
12345671'
```

输入 mi、fa、#sol、la：
```
345#6
```

  ^ 使用实例

  ^ 推广与延伸

^ 参见 ^

+ [音符|Note] + [和弦|Chord] + [时值操作符|DurationOperator] + [强度操作符|IntensityOperator] +