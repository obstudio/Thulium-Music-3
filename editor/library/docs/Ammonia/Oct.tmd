# Oct ((八度))

? Oct(*octave*)
  将当前的基准调性的八度高度设置为*octave*。

^ 具体信息 ^

+ *octave*仅对*key*参数进行修改，但会同步平移所有的*key*参数。
+ 使用 Oct 函数等价于使用 Key 的简记法并使用相应的八度操作符。

! 注意：Oct 函数的效果会被之后的 Key 函数覆盖。

^ 范例
  ^ 基本范例 ^

利用 Oct 函数改变当前八度的位置：
```
<Piano> $1M---| Oct(1) $1M---|
```

Oct 函数等价于在 Key 简记法中使用八度操作符：
```
<Piano> $1M---| (1=C') $1M---|
```

^ 参见 ^

+ [Key] + [KeyShift] + [Con] + [../Benzene/OctShift] +

^ 相关指南 ^

+ [调性|../Guide/Tonality]

