# API: Setting

Setting 是子音轨解析器的一个属性，用来存储子音轨解析中的静态信息。其中包含下列属性：

+ Setting.Key
+ Setting.Bar
+ Setting.Beat
+ Setting.Volume
+ Setting.Speed
+ Setting.Duration
+ Setting.Accent
+ Setting.Light
+ Setting.Stac
+ Setting.Trace

与此同时，Setting 还存储了子音轨的播放效果设置，包含下列属性：

+ Setting.Fade
+ Setting.Reverb
+ Setting.Echo

Setting 具有下列方法：

+ Setting.getOrSetDefault()
+ Setting.extend()
+ Setting.assignSetting()
+ Setting.assignSettingAtIndex()
+ get Setting.effects

此外，用户可以自由地向 Setting 添加属性。
